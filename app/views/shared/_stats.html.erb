<% @user ||= current_user %>
<div class="stats">
  <a href="<%= followable_user_path(@user) %>">
    <strong id="following" class="stat">
     
    </strong>
    following
  </a>
  <a href="<%= followers_user_path(@user) %>">
    <strong id="followers" class="stat">
      
    </strong>
    followers
  </a>
</div>