
<!DOCTYPE html>
<html>
<head>
<style type="text/css">
 div#post_condition{width: 200px; float: right;margin-right: 13%;}
 div#post_image{width: 600px; margin-left: 15%;margin-top: 5%;float: left;}
 div#post_info{margin-top: 5%;margin-left: -40%;}
 section#info{margin-left: 50%;}
</style>
</head>

<body>
<div id="post_info">	
<h1><%= @post.title%></h1>
<section id="info">
<p>Posted by <%= link_to @post.user.name, @user%></p>
<%= time_tag @post.created_at %> 
</section>
</div>
<div id = "clearfix">


	<div id = "post_image">
		<%= image_tag @post.image.url(:medium)%>
		<div id = "content">
			<%= @post.content%>
		</div>
		<p>Reply to the post</p>

 <% @comments.each do |comment| %>
     <%= comment.user.name%><br>
     <%= comment.content%><br>
 <% end -%>
 <div id = "comment_form">
 <%= form_for [@post,@post.comments.build] do |f| %>  
  <p><%= f.text_area :content,:size => '68x10' %> </p>  
  <p><%= f.submit "Post Comment" %> </p>  
<% end %>  
</div>
	</div>
	<div id ="post_condition">
	<%= link_to "Like", like_post_path(@post), method: :get, class: "data" %>
	<i class="fa fa-thumbs-o-up"> </i>
	<%= @post.get_upvotes.size%>
	<%= link_to "Dislike",dislike_post_path(@post), method: :get, class: "data"%><i class="fa fa-thumbs-o-down"><%= @post.get_downvotes.size%></i>
	<br>
	<a href="#Comment_form"><i class="fa fa-comments-o"></i><%= pluralize(@post.comments.count,"Comment")%></a>
   
   <br>

		<% if @post.user == current_user %>
		<%= link_to "Home", root_path%>
		<%= link_to "Edit", edit_post_path(@post)%>
		<%= link_to "Destroy", post_path(@post), method: :delete, data: {confirm: "Are you sure to delete the post?"}%>
		<%- end %>
		<div id = "random-post">
			<br>
			<br>
			<br>
	<p> Random Posts</p>
	<div id = "post">
		<div id = "post_image">
			<%=link_to (image_tag @random_post.image.url(:small)), post_path(@random_post)%>
			
		</div>
		<div id = "post_content">
			<div id = "title">
				<h2><%= link_to @random_post.title, post_path(@random_post)%><h2>
			</div>
			<div id="data.clearfix">
				<p>Posted by <%= @random_post.user.name%></p>
				<p><%= @random_post.comments.count%> Comments  <br><%= @random_post.get_likes.size%>Likes</p>
				<div>
		</div>
	</div>
</div
></div>
</div>
</div>


</body>
</html>






